# 人工操作指南

## 私有仓库创建与授权

1. 由队长登录 GitHub，选择 **New repository**，设置仓库名称。
2. 在 **Visibility** 中选择 **Private**；不要选择 Public，也不要把题面、附件或代码上传到公开仓库。
3. 不勾选 README、`.gitignore` 或许可证，创建一个空的私有仓库。
4. 在仓库 **Settings → Collaborators**（或组织的 **Manage access**）中，仅邀请三名队员的 GitHub 账号；按实际需要授予写入权限，不要发布邀请链接或共享账号密码。
5. 三名队员接受邀请后，人工核对仓库页面仍显示 **Private**，并确认成员名单只有本队人员。将该私有仓库地址作为下文的 `<GitHub仓库地址>`。

仓库私有只限制 GitHub 远程仓库的访问；本地文件仍可能被同步、备份或误发。不要把密码、访问令牌、私钥或其他敏感凭据写入仓库、提交信息或 Codex 提示词。

## 一次工作循环

人工先在 GitHub 创建**空仓库**（不勾选 README、`.gitignore` 或许可证），并把 `<GitHub仓库地址>` 替换为其 HTTPS 或 SSH 地址。首次由一名队员执行：

```powershell
cd 'D:\Users\LENOVO\Desktop\数学建模\国赛团队共享工作区'
git init
git branch -M main
git remote add origin <GitHub仓库地址>
git add .
git commit -m "docs: initialize team workspace"
git push -u origin main
```

其他队员首次使用时，在自己的本地工作位置执行：

```powershell
git clone <GitHub仓库地址> 'D:\Users\LENOVO\Desktop\数学建模\国赛团队共享工作区'
cd 'D:\Users\LENOVO\Desktop\数学建模\国赛团队共享工作区'
```

每次启动 Codex **之前**：

```powershell
cd 'D:\Users\LENOVO\Desktop\数学建模\国赛团队共享工作区'
git status
git pull --ff-only origin main
```

若 `git status` 显示本地修改，先不要 `pull`。人工先决定提交当前修改、暂存修改或与队友协调；不要用强制覆盖命令丢弃内容。

完成一项可共享更新后：

```powershell
git status
git add -- <本次修改的文件或目录>
git commit -m "docs: describe this update"
git push origin main
```

推送被拒绝时：先执行 `git pull --ff-only origin main`；如出现冲突，停止并由涉及同一文件的队员人工处理，确认后再提交推送。

## Codex 本地工作区构建

1. 在 Codex 中打开上述共享目录，而不是临时下载目录。
2. 先让 Codex读取根目录 `协作协议.md`、`人工操作指南.md` 和当前负责分区的 `README.md`。
3. 明确本次唯一目标、允许修改的路径与交接对象；Codex不需要、也不应读取其他分区。
4. 人工检查改动后按“完成更新后”的命令提交和推送。

GitHub 仓库地址、登录、创建远程仓库和访问权限由人工完成；Codex无需直接访问 GitHub。
